# Nginx для skorostivkus.ru с пробросом 6080→80 и 6443→443
# Внутри VPS: listen 80 и 443. Снаружи: :6080 и :6443

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name skorostivkus.ru www.skorostivkus.ru _;

    client_max_body_size 5M;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# После certbot (DNS challenge) — раскомментируйте и настройте редирект HTTP→HTTPS

# server {
#     listen 80 default_server;
#     listen [::]:80 default_server;
#     server_name skorostivkus.ru www.skorostivkus.ru;
#     return 301 https://$host:6443$request_uri;
# }

# server {
#     listen 443 ssl default_server;
#     listen [::]:443 ssl default_server;
#     server_name skorostivkus.ru www.skorostivkus.ru;
#
#     ssl_certificate /etc/letsencrypt/live/skorostivkus.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/skorostivkus.ru/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 5M;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
