# Скопируйте в .env.capacitor перед npm run cap:sync
# IP вашего ПК в локальной сети (ipconfig) — телефон и ПК в одном Wi‑Fi
VITE_API_BASE_URL=http://192.168.0.100:8080/api

# Либо загрузка всего сайта из dev-сервера (без вшитого API):
# CAPACITOR_SERVER_URL=http://192.168.0.100:8080
